# -*- coding: cp1252 -*-
#Didier Salazar    15487
#Esteban Avalos     15059
#El siguiente codigo fue armado tomando de referencia los ejemplos subidos a blackboard.


import simpy
import random

def correr(envir, tiemp, ram, memoria, numeroInstrucciones, vel):
    global tiempoAcumulado
    global tiempo
	
    yield envir.timeout(tiemp)
    print('tiempo actual: %f - procesando... nuevo proceso... utilizar: %d de RAM' % (envir.now, memoria))
    tiempoActual = envir.now
	
    yield ram.get(memoria)
    print('tiempo actual: %f - procesando... proceso admitido... utilizando: %d de RAM' % (envir.now, memoria))
	
    comparar = 0